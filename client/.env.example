# Where the Express + Socket.io server lives. NEXT_PUBLIC_ because the browser
# needs it to call the API and open the socket.
# Read at BUILD time — rebuild after changing it.
NEXT_PUBLIC_CHAT_SERVER_URL=http://localhost:5000
